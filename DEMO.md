# 미니뱅크 데모 — 처음 시연하는 분을 위한 가이드

> 이 프로젝트를 **처음 보는 사람**이 이 문서만 읽고 접속 → 로그인 → 시연까지 할 수 있도록 정리한 실전 가이드입니다.
> (개발/구조 문서는 최상위 `README.md`, `backend-cobol/README.md`, `backend-java/DESIGN.md` 참고)

---

## 0. 이 데모가 뭔가요? (30초 설명)

일본 은행의 **인터넷뱅킹**을 흉내 낸 웹 데모입니다. 로그인해서 **잔액조회·이체·거래명세·대출/상환·공지·계좌개설**을 할 수 있어요.

**진짜 핵심(자랑거리)**: 화면은 평범한 뱅킹 앱처럼 보이지만, 뒤에서는 **메인프레임 관례에 맞춘 문자셋**으로 데이터를 저장합니다.
- **COBOL 백엔드 → Oracle** = **JA16SJIS(Shift-JIS)** 문자셋 DB (일본어 명의가 디스크에 Shift-JIS 바이트로 저장)
- **Java 백엔드 → PostgreSQL** = **UTF-8**
- 앱/화면은 양쪽 다 UTF-8로 통신하지만, DB에 실제로 저장되는 문자셋은 위처럼 다릅니다.

즉 "요즘 웹 화면"과 "메인프레임 문자셋(Shift-JIS)"이 한 몸으로 돌아가는 걸 보여주는 게 이 데모의 포인트입니다. (같은 것을 **COBOL 백엔드**와 **Java 백엔드** 두 버전으로 구현)

> 📌 이전에는 전 컬럼을 메인프레임 바이트(RAW: JEF EBCDIC + COMP-3 + 존10진)로 저장했으나, 2026-07-30에 Oracle=Shift-JIS / PostgreSQL=UTF-8 정상 타입 구조로 마이그레이션했습니다.

---

## 1. 접속하기 — 둘 중 하나

### 옵션 A. 라이브 서버 (제일 쉬움 · 설치 필요 없음) ⭐추천
1. 사내 **Tailscale**를 켜고 로그인 (권한은 담당자에게 요청)
2. 브라우저에서 접속: **`http://<서버주소>:8092/`** (COBOL 화면) / **`http://<서버주소>:8081/`** (Java 화면)
   - 서버주소는 담당자에게 문의 (보안상 문서에 안 적음)
3. 바로 §2 로그인으로.

### 옵션 B. 내 PC에서 직접 띄우기 (Docker 필요)
**Docker Desktop**이 설치·실행 중이어야 합니다. 그다음 둘 중 하나:

**B-1) COBOL 백엔드(정본)** — 단, 사전 준비물 필요
```bash
# 먼저 backend-cobol/docker/vendor/README.md 보고 Oracle Instant Client·GixSQL 다운로드
docker compose -f backend-cobol/docker/compose.asis.yml up -d --build
# → http://localhost:8092/   (COBOL. 최초 기동은 Oracle XE 초기화로 10~20분)
```

**B-2) Java 백엔드** — 준비물 없이 바로 됨 (가장 간단)
```bash
# 리포지토리 루트에서 (COBOL과 동일한 방식)
docker compose -f backend-java/compose.java.yml up -d --build
# → http://localhost:8081/     (끄기: docker compose -f backend-java/compose.java.yml down)
```

> 어느 쪽이든 화면·기능은 동일합니다. **잘 모르면 옵션 A(라이브) 또는 B-2(Java)** 를 쓰세요.

---

## 2. 로그인 계정

| 점번(店番) | 계좌번호 | 비밀번호 | 명의 | 용도 |
|------|----------|----------|------|------|
| **001** | **1000123** | **ks1234** | 山田太郎 | ⭐ 기본 시연용(거래·프로필 있음) |
| 200 | 2000456 | 1111 | 佐藤花子 | 이체 상대 등 |
| 305 | 3000789 | 1234 | 髙橋圭子 | |
| 210 | 5001415 | 1234 | 渡辺健 | **동결(凍結) 계좌** — 거부 시연용 |

> 화면은 일본어입니다. 로그인 폼: **店番 / 口座番号 / パスワード** 순서.

---

## 3. 시연 시나리오 (5~7분 코스, 순서대로)

**① 로그인** — `001 / 1000123 / ks1234`
- 로그인되면 명의 **山田太郎**, 잔액 **523,400엔**이 보입니다. (일본어 명의가 깨지지 않고 제대로 나오는 게 포인트)

**② 잔액조회 / 보유계좌**
- 잔액·종별(普通=보통예금)·상태 확인.

**③ 이체(振込)** — 가장 임팩트 있는 데모
- 받는 계좌 **2000456**, 금액 **10,000** 입력 → 실행
- **수수료 110엔**이 붙고, 잔액이 `523,400 − 10,000 − 110 = 513,290`으로 줄어드는 것 확인
- 포인트: 출금·입금·수수료가 **하나로 묶여(원자적)** 처리됨.

**④ 거래명세(明細)**
- 방금 이체가 목록 맨 위에 뜨고, **거래 후 잔액이 누적 계산**돼 보입니다.

**⑤ 대출(ローン) → 상환(返済)**
- 대출 실행: 금액 50만 / 방식 A / 기간 5년 → 계좌에 50만 입금 반영
- 상환: 원금 일부 입력 → **이자·수수료(550엔)** 계산되어 차감

**⑥ 공지(お知らせ)**
- 공지 목록 보기 → 새 공지 등록해보기

**⑦ 신규 계좌 개설(開設)**
- 명의(예: 한자 이름)·비밀번호 입력 → 새 계좌번호(9000001~) 발급 → 그 계좌로 로그인

---

## 4. "진짜 자랑거리" 보여주기 (기술 어필용, 선택)

화면만 보면 평범하니, **두 DB의 문자셋이 다름**(Oracle=Shift-JIS / PostgreSQL=UTF-8)을 보여주면 임팩트가 있습니다.

1. **DBeaver**(또는 아무 DB 툴)로 접속
   - COBOL판: Oracle `<서버 또는 localhost>:1522 / XEPDB1`, 계정 `minibank / minibank` (문자셋 JA16SJISTILDE)
   - Java판: PostgreSQL `<서버 또는 localhost>:5434 / minibank`, 계정 `minibank / minibank` (문자셋 UTF-8)
2. `KOUZA` 테이블 조회 → 명의(山田太郎)·금액(523400)이 **정상 타입 컬럼**(VARCHAR2/NUMBER · varchar/bigint)에 읽는 값 그대로 저장돼 있음
3. 설명: "화면·앱은 UTF-8로 통신하지만, Oracle DB는 디스크에 **Shift-JIS(JA16SJIS)** 로, PostgreSQL은 **UTF-8** 로 저장합니다. 드라이버가 문자셋 변환을 처리해 어느 쪽이든 일본어가 깨지지 않고 표시됩니다."
   - (Oracle에서 실제 저장 바이트를 보고 싶으면 `SELECT DUMP(meigi_kanji) FROM kouza;` 로 Shift-JIS 바이트를 확인할 수 있음.)

**야간배치 시연**(선택): Java판이면 아래 한 줄로 일일 배치(이자 가산·明細·집계 리포트) 실행 결과를 볼 수 있습니다.
```bash
curl -X POST http://localhost:8081/api/batch/run
ls backend-java/data/          # 帳票 7종 (MEISAI.TXT + NIPPO/ZANDAKA/TESURYO/KYUMIN/KOUZA.LST/TOKEI)
cat backend-java/data/MEISAI.TXT   # 明細: D=거래별 取引後残高 / T=계좌별 利息·手数料合計·確定残高
```

**COBOL과 값이 같은지 보여주기**(선택): 두 스택을 다 띄운 상태에서
```bash
sh tools/parity/compare.sh     # => PARITY OK  (帳票 7종 diff 무차이)
```

---

## 5. 에러/엣지 시연 (원하면)

- **동결 계좌**: `210 / 5001415 / 1234`로 로그인하거나 이 계좌에서 이체 시도 → **거부**(account_frozen)
- **잔액 부족**: 잔액보다 큰 금액 이체 시도 → **거부**(insufficient_funds)
- **대출 한도**: 300만엔 초과 대출 시도 → **거부**(loan_over_limit)

---

## 6. 막히면 / 문의

- 접속 안 됨(라이브): Tailscale 로그인 상태·서버주소 확인 → 담당자 문의
- 로컬이 안 뜸: Docker Desktop 실행 여부 확인, 포트(8092/8081/5434/1522) 충돌 확인
- COBOL 화면은 뜨는데 API가 `db_connect_failed`: Oracle 기동 대기 중이거나(최초 10~20분),
  서버라면 `oracle_sjis` 컨테이너 IP가 바뀐 것 → `README.md` §4.4 의 3) 재실행
- 그 외 전부: **담당자 seoyeong (seoyeong.jeong@ks-infosys.com)**

> ⚠️ 이 저장소는 공개(public)입니다. 서버 주소·접속 키·실제 비밀번호 등 민감정보는 문서에 적지 말고 담당자에게 직접 받으세요.
