<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ksbank.minibank.strict.online.dao.LoanDao">

    <resultMap id="select_01_resultMap" type="com.ksbank.minibank.strict.online.dto.CLoanDto">
        <result property="C_ID" column="loan_id"/>
        <result property="C_PRIN" column="principal"/>
        <result property="C_BAL" column="balance"/>
        <result property="C_METHOD" column="method"/>
        <result property="C_YEARS" column="term_years"/>
    </resultMap>

    <select id="select_01" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto" resultMap="select_01_resultMap"><![CDATA[
        SELECT loan_id, principal, balance, method, term_years
          FROM loan_asis WHERE status = #{HV_ACT} AND kouza_no = #{HV_KOUZA} ORDER BY loan_id
    ]]></select>

    <select id="select_02" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto" resultType="long"><![CDATA[
        SELECT COALESCE(SUM(balance),0) FROM loan_asis
         WHERE status = #{HV_ACT} AND kouza_no = #{HV_KOUZA}
    ]]></select>

    <select id="select_03" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto" resultType="long"><![CDATA[
        SELECT COUNT(*) FROM kouza WHERE kouza_no = #{HV_KOUZA}
    ]]></select>

    <resultMap id="select_04_resultMap" type="com.ksbank.minibank.strict.online.dto.LoanDto">
        <result property="HV_LOANID" column="hv_loanid"/>
        <result property="HV_TID" column="hv_tid"/>
    </resultMap>

    <select id="select_04" resultMap="select_04_resultMap"><![CDATA[
        SELECT nextval('seq_loan_asis') AS hv_loanid, nextval('seq_torihiki') AS hv_tid
    ]]></select>

    <insert id="insert_01" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto"><![CDATA[
        INSERT INTO loan_asis
          (loan_id, kouza_no, principal, balance, method,
           term_years, rate, status)
        VALUES (#{HV_LOANID}, #{HV_KOUZA}, #{HV_AMT}, #{HV_BAL}, #{HV_METHOD}, #{HV_YEARS}, 2.5, #{HV_ACT})
    ]]></insert>

    <select id="select_05" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto" resultType="long"><![CDATA[
        SELECT zandaka FROM kouza WHERE kouza_no = #{HV_KOUZA}
    ]]></select>

    <update id="update_01" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto"><![CDATA[
        UPDATE kouza SET zandaka = #{HV_ZAN} WHERE kouza_no = #{HV_KOUZA}
    ]]></update>

    <insert id="insert_02" parameterType="com.ksbank.minibank.strict.online.dto.LoanDto"><![CDATA[
        INSERT INTO torihiki
          (torihiki_id, kouza_no, torihiki_dt, torihiki_kbn,
           kingaku, aite_kouza, tesuryo, tekiyou)
        VALUES (#{HV_TID}, #{HV_KOUZA}, #{HV_DT}, '1', #{HV_AMT}, NULL, NULL, NULL)
    ]]></insert>

</mapper>
